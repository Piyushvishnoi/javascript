function myfun(number) {
  //no will be compared to a no then it will be true let if we pass a stringthen we got false
  //here we don't have any type conversion 
  if (number === 2) {
    return false;
  } else return true;
}
console.log(myfun(20));
