function myfun(number) {
  ////comparision using equity operator
  //here string and number can be compared that means type conversion is possible
  if (number == 2) {
    return false;
  } else return true;
}
console.log(myfun(20));
